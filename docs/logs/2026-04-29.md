# 2026-04-29 작업 로그

## 브랜치
`fix/easy-bugs-and-login` (feature/ai-emotion-analysis 기반)

## 완료된 작업

### 1. M-003 라우팅 버그 수정 (fix/diary)
- **증상**: AI 채팅 일기 저장 후 상세 페이지로 이동되지 않고 단순 뒤로가기만 됨
- **원인**: `_saveDiary`가 `void` 반환 + `Navigator.pop` 두 번
- **해결**: `_saveDiary → Future<String>` (entryId 반환), 저장 후 `context.go('/diaries/$id')`

### 2. 이메일/비밀번호 자체 로그인 추가 (feat/auth)
- `AuthService.signInWithEmail`, `signUpWithEmail`, `sendPasswordResetEmail` 추가
- `FirebaseAuthException` → 한글 에러 메시지 변환
- `AuthProvider` 대응 메서드 추가
- `LoginPage` 개선: 이메일 폼 + "또는" 구분선 + Google 로그인 순서
- `SignUpPage` 신규 생성 (닉네임/이메일/비밀번호/확인)
- 비밀번호 찾기 다이얼로그 추가
- `/signup` 라우트 추가

### 3. UI 전면 개편 (feat/ui)
- **색상 테마**: 라일락 퍼플/핑크 → 세이지 그린(#6B8F71) + 웜 클레이(#AB8B6B)
- **배경**: 차가운 슬레이트 → 따뜻한 파피루스(#FAF8F5)
- **다크 테마**: 차가운 다크 → 따뜻한 다크 그린-블랙(#1A1C18)
- **탭바**: `BottomNavigationBar` → M3 `NavigationBar` 전환
- **앱 아이콘**: `psychology` → `auto_stories_rounded` (일기 느낌)
- **LoginHeader**: 로고 아이콘 변경, 부제목 자연스럽게 수정

### 4. VoiceChatPage 신규 생성
- 마이크 버튼(중앙), 파동 링 애니메이션, 채팅 버블 UI
- 청취/처리/말하기 상태 시각화
- `/ai/voice` 라우트 추가
- STT(Soniox)/TTS 연동 준비 (`_simulateListening` 플레이스홀더)

## 커밋 목록
1. `fix(diary): AI 채팅 일기 저장 후 상세 페이지로 이동 수정`
2. `feat(auth): 이메일/비밀번호 자체 로그인 및 회원가입 추가`
3. `feat(ui): 테마 전면 개편 + 음성 AI 대화 페이지 추가`

## 다음 작업 (다음 세션)
- Provider → Firebase 직접 접근 제거 (Clean Architecture)
- 카카오 로그인 추가
- AI 페이지에 VoiceChatPage 진입 버튼 추가
- 홈 화면 UI 추가 개선 (1884줄 → 분리 필요)
